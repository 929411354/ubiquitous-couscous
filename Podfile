# 注意：必须删除所有中文和特殊符号
platform :ios, '13.0'

# 关键修改：使用英文标识符替换中文（如截图中的"适用于iOS的"）
target 'iOSApp' do  # 使用英文标识符
  # 基础依赖库（保持简洁）
  pod 'Alamofire'  # 示例
  
  # 绝对不要添加以下字符：
  # - 中文/特殊符号：适用于iOS的、©、®
  # - 空格：pod ' name' 
  # - 注释中写：`def 方法`（避免反引号）

  use_frameworks!
end