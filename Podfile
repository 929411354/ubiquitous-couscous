# 重要说明：此文件必须放在 iOS 项目目录
platform :ios, '13.0'  # 根据项目调整版本

# 项目名称需与截图完全一致（含中文字符）
target '适用于iOS的' do
  # 添加项目核心依赖库（根据实际需求修改）
  pod 'Alamofire', '~> 5.6'      # 网络请求
  pod 'SnapKit', '~> 5.6'         # 自动布局
  pod 'Kingfisher', '~> 7.0'      # 图片加载
  
  # 启用动态框架（必需）
  use_frameworks!
end
